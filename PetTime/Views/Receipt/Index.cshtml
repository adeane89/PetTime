@model PetTime.Models.PetOrder

@{
    ViewData["Title"] = "Order" + Model.ID;
}
<div class="w3-content w3-container w3-padding-64">
    <h2 class="w3-center">Thanks for ordering!</h2>
    <div class="w3-row">
            <table class="table">
                <thead>
                    <tr>
                        <th>Puppy</th>
                        <th>Visit</th>
                        <th>Length of Visit</th>
                        <th>Price</th>
                        <th>Total Price</th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model.PetOrderProducts)
                    {
                        <tr>
                            <td>@product.ProductName</td>
                            <td>@product.StartDate</td>
                            <td>@product.ProductLength</td>
                            <td>@product.ProductAnimalCount</td>
                            <td>@((product.ProductAnimalCount * product.ProductPrice).ToString("c"))</td>
                        </tr>
                    }
                </tbody>
                <tfoot>
                    <tr>
                        <th colspan="4">Total</th>
                        <td>@(Model.PetOrderProducts.Sum(x => x.Quantity * x.ProductPrice).ToString("c"))</td>
                    </tr>
                </tfoot>
            </table>
    </div>
</div>
